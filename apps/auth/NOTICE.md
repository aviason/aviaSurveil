# Notices

The provider scaffold directly depends on:

- `github.com/zitadel/oidc/v3` v3.47.5, Apache-2.0. Source and license:
  <https://github.com/zitadel/oidc/tree/v3.47.5>.

The complete resolved dependency graph is pinned in `go.sum`. Before a
release, dependency license and notice scanning must be rerun and this notice
must be reconciled with the generated SBOM.
