import { test } from "@playwright/test";

test.describe("governed checklist intake (mock)", () => {
  test.skip("blocked: full Admin intake route is candidate-only until the real parser/object-store profile is authorized", async () => {
    // Kept as an explicit discovery point; no AGA bytes or browser state are
    // synthesized into a release claim.
  });
});
